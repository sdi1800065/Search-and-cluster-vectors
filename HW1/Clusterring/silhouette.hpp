

void compute_silhouette();                                            //Computes silhouette
int find_second_closest_centroid(int point, int current_cluster);     //FInd the decond best centroid of a vector-point (besides the one that it is its own)
